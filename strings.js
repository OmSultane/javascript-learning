const name="om"

const age = 18

console.log(`hello my name is ${name} and my age is ${age}`);

let n = new String('Om-Sultane') //we can create string using this constructor also it provides so "many methods" that's the reason of using it

console.log(`the length of name ${n} is ${n.indexOf('S')}`);

console.log(n.includes('-'))

console.log(n.split('-'));
